---
name: format-wechat-articles
description: Convert Chinese articles and uploaded Word/DOCX manuscripts into polished WeChat Official Account layouts with content-aware color selection, WeChat-compatible inline styles, embedded images, a one-click rich-text copy page, and a clean HTML page. Use when the user asks to 排版公众号文章、生成可复制富文本、生成公众号HTML、沿用公众号样式、根据稿件自动配色，or turn a manuscript into a design-forward WeChat article.
---

# 公众号智能排版

把稿件转化为简洁、大气、有设计感且可稳定复制到微信公众号后台的排版。保持内容准确，以文章主题决定视觉系统，不把所有稿件机械套入同一种颜色。

## 必须遵循的原则

1. 以用户最新上传或指定的稿件为唯一正文来源。
2. 保留标题、正文、图片顺序、重点数据、署名和日期。只清理多余空格和明显的排版符号；未经要求不要改写事实或删减正文。
3. 用户指定颜色或风格时优先执行；未指定时按文章主题自动选色。
4. 使用微信公众号更容易保留的行内样式。正文区域不得依赖外部 CSS、外部字体、复杂定位、伪元素、CSS Grid 或背景图片。
5. 控制设计密度。用标题区、章节标题、1–3种重点模块和结尾模块建立层级，避免每段都装进卡片。
6. 默认交付两份文件：
   - 一键复制富文本版：带操作栏和“复制全部富文本”按钮。
   - HTML纯净版：不含操作栏和脚本，便于导入或二次编辑。

## 工作流程

### 1. 读取并核对稿件

- 对 DOCX 使用文档读取能力检查全部段落、图片、表格、页眉页脚、署名和日期。
- 确认图片数量及其在正文中的位置，不沿用旧稿已经删除的图片或内容。
- 对修改稿，以最新文件为准，不从之前版本补回内容。
- 提取后建立内容清单：标题、导语、正文板块、数据、引语、图片、结语、署名日期。

### 2. 选择视觉主题

先读取 [references/design-system.md](references/design-system.md)。应用以下优先级：

1. 用户明确指定的颜色、品牌色或参考风格。
2. 稿件的主导发布场景与主题。
3. 自动配色规则。

当一篇稿件同时涉及多个主题时，以标题和发布目的为主。例如，AI企业调研稿可使用蓝紫科技色；正式政策通知则优先使用政务蓝。

### 3. 规划版式

- 长标题允许通过 `<br>` 做自然断行，但文字与标点必须保持一致。
- 导语可使用顶部重点卡片。
- 章节标题使用清晰编号或短英文标签，不堆叠装饰。
- 数据适合使用双列或多列指标块；流程适合使用标签串；负责人表态适合使用引语卡。
- 图片默认全宽展示，避免在手机端并排缩得过小。
- 署名和日期放在文章末尾右对齐。
- 结尾可用一句克制的主题收束，不新增未经稿件支持的事实。

### 4. 生成HTML

优先使用 `scripts/wechat_html_builder.py`：

```bash
python3 scripts/wechat_html_builder.py plan.json
```

计划文件的字段和模块格式见 [references/design-system.md](references/design-system.md)。图片可以传入本地路径，也可以直接指定 DOCX 路径和压缩包内的图片成员，从而内嵌为 data URI。

如果稿件需要脚本尚未支持的特殊模块，可在保持同一设计令牌和兼容性约束的前提下扩展脚本，不要临时生成一套完全不同的样式。

### 5. 质量检查

交付前必须检查：

- 两份 HTML 都能解析，且只各有一个 `wechat-article` 主体。
- 标题、每个非空正文段落、数据、网址、署名和日期均被包含。
- 图片数量与稿件一致，内嵌图片字节与原图一致。
- 正文所有视觉元素使用行内样式，不含外部样式表。
- 富文本版含复制按钮；纯净版不含脚本。
- 正文不存在明显横向溢出风险，图片宽度不超过 100%。
- 文件名清晰，且富文本版和纯净版容易区分。

## 配色与内容判断

自动配色只是起点。根据稿件气质调整模块密度：

- AI、科技、OPC、数字化：蓝紫、深蓝，强调数据和平台能力。
- 政务调研、领导交流、政策发布：政务蓝、深蓝灰，减少装饰。
- 教育、学校、青少年：蓝绿、清新蓝，增强亲和力。
- 党建、廉洁、红色主题：红色、红金，保持庄重。
- 文化、艺术、传承：暖棕、米金或低饱和红。
- 文旅、乡村、生态：绿色、暖橙或自然色。
- 商务合作、战略签约：深蓝、灰蓝、克制金色。

详细色值、字号和兼容模块见 [references/design-system.md](references/design-system.md)。

## 常用触发方式

- “按公众号格式排版这个Word，颜色根据内容自行判断。”
- “沿用之前的公众号样式，生成可复制富文本和HTML。”
- “这篇采用红金色，排版不要太花哨。”
- “保留图片、署名和日期，生成公众号后台可复制版本。”
