---
name: format-wechat-articles
description: Convert Chinese Word/DOCX, Markdown, plain text, screenshots, links, or existing HTML into polished WeChat-compatible rich text and clean HTML. Use for 公众号智能排版、微信公众号HTML、可复制富文本、先推荐三套风格、按文章类型自动匹配版式和颜色、自动识别标题金句要点与段落层级、参考截图或链接排版、现代品牌专题风与UI感排版。Model-agnostic instructions can also be adapted for Chinese LLMs such as Doubao, Qwen, Kimi and ERNIE. Default to a fast read-recommend-confirm-build workflow; prioritize semantic hierarchy, aesthetic quality and structural variety without slowing routine work.
---

# 公众号智能排版

把稿件转化为现代、清晰、有设计完成度且可稳定复制到微信公众号后台的排版。默认采用“轻流程＋强审美”：稿件只读取一次，先推荐三套真正不同的方向，用户选择后只生成一套。

本 Skill 的排版方法、语义识别规则和设计规范不绑定单一模型。除 ChatGPT、Codex 外，也可将 `SKILL.md` 与配套资源导入或提供给豆包、通义千问、Kimi、文心一言等支持长提示词和文件处理的中国大模型使用；不同平台的 DOCX/图片读取、Python 执行和 HTML 文件输出能力不同，缺少相应工具时应按平台能力降级执行。

## 核心原则

1. 以用户最新上传或指定的稿件为唯一正文来源。保留标题、事实、数据、网址、图片及其顺序，不从旧稿带回内容，不虚构信息。
2. 允许把原段落拆分为导语、日程、数据、引语、步骤或信息卡，也允许从原文提炼简洁的排版性小标题；不得改变事实、遗漏内容或引入新结论。
3. 先判断文章场景，再识别标题层级、导语、金句、核心判断、并列要点、数据、流程、时间、引语、图片和普通段落；场景决定整篇气质，内容语义决定具体组件。
4. 视觉组件必须由内容语义触发。没有相应内容时，不得为了显得丰富而虚构金句、数据、步骤、标签或卡片；普通正文保持纯文字阅读。
5. 不同类型不能只换颜色。首屏、章节、重点模块、图片节奏和结尾结构必须共同服从场景与内容层级。
6. 好看是底线，场景差异不能以牺牲完成度为代价。若新结构明显弱于已有成熟样式，应重新组合成熟组件。
7. 禁止无差别复用同一整套模板，但不禁止渐变首屏、色线标题、圆角卡片、数字模块等成熟组件；这些组件只在内容和场景适合时使用。
8. 同批次三套方案，任意两套必须在“首屏、章节标题、金句/要点处理、图片节奏、结尾/信息指南”中至少三项不同。仅换颜色、圆角或装饰线不算不同方案。
9. 用户提供链接、截图、HTML或以前的成品时，优先提炼其配色关系、字号层级、边界、留白、模块节奏和整体气质，做相近气质的原创实现。
10. 使用微信公众号更容易保留的行内样式，不依赖外部CSS、网络字体、复杂定位、CSS Grid、动画或外部背景图片。
11. JSON只作为内部构建数据，成品页面不得出现JSON、构建计划、代码块或调试信息。

## 快速模式（默认）

### 1. 只读取一次稿件

一次性读取标题、正文、图片、表格、数据、时间、引语和流程。图片按原位置保留，不为单项内容重复扫描。

### 2. 做内容识别与五项快速判断

读取 [references/semantic-structure.md](references/semantic-structure.md)，在内部完成一次内容地图，不向用户展示JSON或分析过程。判断：

- 主场景 `scene`；
- 主要读者与发布目的；
- 以文字、图片、数据还是流程为主；
- 设计密度 `light`、`balanced` 或 `rich`。
- 标题层级、导语、金句、核心判断、并列要点、数据、流程、引语、图注和普通段落分别位于哪里。

场景和可选风格见 [references/layout-library.md](references/layout-library.md)。不默认搜索网页；只有内置场景无法覆盖或用户明确要求时才读 [references/case-patterns.md](references/case-patterns.md) 并研究公开案例。

### 3. 推荐三套方案并暂停

读取 [references/recommendation-system.md](references/recommendation-system.md)，用固定字段推荐：

| 方案 | 场景与风格 | 首屏 | 章节层级 | 金句与要点 | 图片节奏 | 配色 | 推荐理由 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A 推荐 | 具体场景＋风格名 | 具体构图 | 一级与二级标题如何区分 | 识别出的重点如何呈现 | 具体方式 | 主色、辅助色、强调色 | 与目的、读者和素材的匹配 |

三套只做文字方案，不提前生成三套HTML。推荐后固定提示：

> 请选择A、B或C，也可以说“A版式＋B配色”。如果你有喜欢的公众号文章、排版截图、参考链接或以前使用过的样式，也可以直接发给我。我会提取其配色、字体层级、标题样式、留白、卡片结构和图片节奏，为本篇生成相近气质的原创方案。

停止生成，等待用户选择。

### 4. 只生成用户选中的一套

读取 [references/design-system.md](references/design-system.md)，明确：

- `scene`
- `layout`
- `theme`
- `density`
- `hero_variant`
- `section_variant`
- `metrics_variant`
- `steps_variant`
- `emphasis_variant`
- `points_variant`

优先使用：

```bash
python3 scripts/wechat_html_builder.py plan.json
```

默认交付：

- 一键复制富文本版；
- HTML纯净版。

## 审美底线

- 首屏必须有明确的第一视觉，标题不能淹没在装饰里。
- 先用字号、字重、留白和对齐建立层级，再使用颜色、边框、渐变和卡片。
- 正文形成深色、浅色、白底三层节奏；正式稿可以减少深色面积，但仍要有视觉锚点。
- 章节标题通常控制在8–16个汉字；不要把整句正文摘要当作标题。
- 一级章节、二级小标题和普通加粗句必须有明显但克制的视觉差异。
- 全文通常设置2–5个视觉锚点，只使用一套主要圆角和边界语言。
- 小标签、英文眉题、虚线、图标不能同时堆叠；英文只在适合的品牌、科技、活动或杂志风中使用。
- 卡片只用于成组信息，不包裹普通正文。
- 可在首屏、金句或关键数据中使用1–2处克制渐变，并设置纯色回退；不默认蓝紫渐变，也不使用“渐变＋发光＋多标签＋满屏卡片”的堆砌。
- UI设计感来自信息分区、索引、状态条、数据面板和稳定对齐，不来自模拟按钮或制造不能点击的控件。
- 同一场景可以使用成熟组件，但连续文章不得无脑复用同一套首屏、章节和重点模块。
- “夏夜品牌专题”可采用深色品牌首屏、浅色导语卡、日程模块、单个深色数据锚点、简洁色线标题和圆角参与指南；它是生活活动类的质量基准之一，不是所有文章的通用模板。

## 一次性快速验收

只在成品完成后检查一次：

1. 标题、正文、数据、网址和图片是否完整，是否出现虚构信息。
2. 标题层级、金句、要点和普通段落是否识别准确；首屏是否有明确视觉中心，全文是否有深浅节奏。
3. 是否存在三处以上无意义虚线、小标签堆叠、混杂圆角或每段卡片化。
4. 每个重点组件是否有原稿语义依据，最终版式是否符合所选场景和变体，而不是只改变颜色。
5. 两份HTML是否可解析；富文本版只复制文章主体，纯净版不含脚本；无外链样式和明显横向溢出。

检查通过即交付，不重复执行规划测试。

## 深度模式

只在用户明确要求搜索、提供公众号名称、现有三套方案不满意或场景超出内置库时，研究2–4个近期公开案例。检索只服务于设计判断，不把来源不明的内容加入正文。
